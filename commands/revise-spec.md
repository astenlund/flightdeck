---
description: "Use when a design-shaped file (feature, pattern, or bug-investigation doc) has been written or substantially revised and needs hardening before planning."
---

# revise-spec

Invoke the `flightdeck:revise` skill with artifact type `spec` and scope `$ARGUMENTS`. This command exists as a familiar short form; the skill owns the entire procedure.
