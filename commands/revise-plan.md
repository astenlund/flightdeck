---
description: "Use when an implementation plan has been written and needs hardening before execution begins."
---

# revise-plan

Invoke the `flightdeck:revise` skill with artifact type `plan` and scope `$ARGUMENTS`. This command exists as a familiar short form; the skill owns the entire procedure.
