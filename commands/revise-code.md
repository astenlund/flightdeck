---
description: "Use when a code change (diff, staged work, or named files) is ready for deep multi-agent review before it ships."
---

# revise-code

Invoke the `flightdeck:revise` skill with artifact type `code` and scope `$ARGUMENTS`. This command exists as a familiar short form; the skill owns the entire procedure.
